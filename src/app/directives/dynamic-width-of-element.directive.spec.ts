import { DynamicWidthOfElementDirective } from './dynamic-width-of-element.directive';

describe('DynamicWidthOfElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicWidthOfElementDirective();
    expect(directive).toBeTruthy();
  });
});
